<!doctype html>
<html lang="es">

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title><%= title %> - Tu Aplicación</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-LN+7fdVzj6u52u30Kp6M/trliBMCMKTyK833zpbD+pXdCLuTusPj697FH4R/5mcr" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css"
        integrity="sha512-Fo3rlrojF6p+YwUv0D9Q3a/J3f/n8u/S3qfN/nF1x7qC/n6K0d4T5r5s/fF2a3+L/qV0G8q1d7Q/cQ1kQ2w=="
        crossorigin="anonymous" referrerpolicy="no-referrer" />
    <style>
        body {
            background: linear-gradient(to right, #6a11cb 0%, #2575fc 100%); /* Fondo degradado consistente */
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            color: #333;
            min-height: 100vh;
            padding-top: 2rem; /* Espacio superior */
            padding-bottom: 2rem; /* Espacio inferior */
        }

        .header-section {
            background-color: #fff;
            border-radius: 1rem;
            padding: 2rem;
            margin-bottom: 2rem;
            box-shadow: 0 0.5rem 1.5rem rgba(0, 0, 0, 0.1);
            text-align: center;
        }

        .header-section h1 {
            color: #2575fc; /* Color del degradado */
            font-weight: 700;
            margin-bottom: 1.5rem;
        }

        .btn-group-custom .btn {
            border-radius: 0.5rem;
            padding: 0.75rem 1.5rem;
            font-size: 1rem;
            margin: 0 5px; /* Pequeño espacio entre botones */
        }

        .card-custom {
            border-radius: 1rem;
            overflow: hidden; /* Asegura que la imagen tenga bordes redondeados */
            box-shadow: 0 0.25rem 0.75rem rgba(0, 0, 0, 0.1);
            border: none; /* Eliminar borde por defecto */
            transition: transform 0.2s ease-in-out;
            height: 100%; /* Asegura que todas las tarjetas tengan la misma altura si el contenido lo permite */
            display: flex;
            flex-direction: column;
        }

        .card-custom:hover {
            transform: translateY(-5px); /* Pequeño efecto hover */
        }

        .card-custom .card-img-top {
            height: 200px; /* Altura fija para la imagen */
            object-fit: cover; /* Recorta la imagen para que encaje */
            width: 100%;
        }

        .card-custom .card-body {
            padding: 1.5rem;
            display: flex;
            flex-direction: column;
            flex-grow: 1;
        }

        .card-custom .card-title {
            font-size: 1.5rem;
            font-weight: 600;
            margin-bottom: 0.75rem;
            color: #333;
        }

        .card-custom .card-text {
            font-size: 1rem;
            color: #555;
            margin-bottom: 0.5rem;
        }

        .card-custom .card-footer {
            background-color: #f8f9fa;
            border-top: 1px solid #e9ecef;
            padding: 1rem 1.5rem;
            font-size: 0.9rem;
            color: #6c757d;
        }

        .no-image-placeholder {
            height: 200px;
            background-color: #e9ecef;
            display: flex;
            justify-content: center;
            align-items: center;
            color: #6c757d;
            font-size: 1.5rem;
            border-top-left-radius: 1rem;
            border-top-right-radius: 1rem;
        }

        .back-link {
            text-align: center;
            margin-top: 2rem;
        }
    </style>
</head>

<body>
    <div class="container py-4">
        <div class="header-section">
            <h1 class="display-4"><%= title %></h1>
            <div class="btn-group-custom">
                <a href="/casas/mias" class="btn btn-primary">
                    <i class="fas fa-home me-2"></i>Ver mis casas
                </a>
                <a href="/casas" class="btn btn-secondary">
                    <i class="fas fa-list me-2"></i>Ver todas las casas
                </a>
                <a href="/auth/logout" class="btn btn-danger">
                    <i class="fas fa-sign-out-alt me-2"></i>Cerrar Sesión
                </a>
            </div>
        </div>

        <div class="row row-cols-1 row-cols-md-2 row-cols-lg-3 g-4">
            <% if (casas && casas.length > 0) { %>
            <% casas.forEach(casa => { %>
            <div class="col">
                <div class="card card-custom">
                    <% if (casa.imagenURL) { %>
                    <img src="<%= casa.imagenURL %>" class="card-img-top" alt="Imagen de <%= casa.nombre %>">
                    <% } else { %>
                    <div class="no-image-placeholder">
                        <i class="fas fa-image fa-2x"></i>
                    </div>
                    <% } %>
                    <div class="card-body">
                        <h5 class="card-title"><%= casa.nombre %></h5>
                        <p class="card-text"><strong><i class="fas fa-dollar-sign me-1"></i>Valor:</strong> <%= casa.valor.toLocaleString('es-ES', { style: 'currency', currency: 'USD', minimumFractionDigits: 0, maximumFractionDigits: 0 }) %></p>
                        <p class="card-text"><strong><i class="fas fa-user-tag me-1"></i>Propietario:</strong> <%= casa.username ?? 'Anónimo' %></p>
                    </div>
                    <div class="card-footer">
                        ID: <%= casa.id %>
                    </div>
                </div>
            </div>
            <% }) %>
            <% } else { %>
            <div class="col-12 text-center text-white-50 py-5">
                <p class="lead">No hay casas disponibles para mostrar.</p>
                <i class="fas fa-house-chimney fa-4x"></i>
            </div>
            <% } %>
        </div>

        <div class="back-link">
            <a href="/auth/sesion" class="btn btn-light btn-lg mt-5">
                <i class="fas fa-arrow-left me-2"></i>Volver a la Sesión
            </a>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-ndDqU0Gzau9qJ1lfW4pNLlhNTkCfHzAVBReH9diLvGRem5+R9g2FzA8ZGN954O5Q"
        crossorigin="anonymous"></script>
</body>

</html>